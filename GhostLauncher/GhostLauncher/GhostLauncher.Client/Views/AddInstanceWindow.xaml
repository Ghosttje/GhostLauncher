<Window x:Class="GhostLauncher.Client.Views.AddInstanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Instance" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" ShowInTaskbar="False" ResizeMode="NoResize" SizeToContent="WidthAndHeight">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../ResourceDictionaries/ResourceDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <StackPanel Orientation="Horizontal">
        <Button Command="{Binding AddInstanceCommand}">
            <StackPanel Orientation="Vertical">
                <Image Height="256" Style="{StaticResource AddInstance}" />
                <Button Content="Add instance" />
            </StackPanel>
        </Button>
        <Button Command="{Binding AddServerCommand}">
            <StackPanel Orientation="Vertical">
                <Image Height="256" Style="{StaticResource AddServer}" />
                <Button Content="Add server instance" />
            </StackPanel>
        </Button>
    </StackPanel>
</Window>
